# Hearth — Environment Example
# Settings are configured via the in-app Settings page (stored in localStorage).
# This file is for reference only — no runtime env vars are required.

# If self-hosting behind a reverse proxy with a custom base path, set:
# VITE_BASE_URL=/hearth
